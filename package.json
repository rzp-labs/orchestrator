{
  "name": "orchestrator",
  "version": "0.1.0",
  "description": "AI-powered orchestrator for tactical product development",
  "private": true,
  "devDependencies": {
    "shfmt": "^3.8.0"
  },
  "scripts": {
    "format-sh": "shfmt -i 4 -w scripts/*.sh",
    "check-sh": "shfmt -d scripts/*.sh"
  }
}
